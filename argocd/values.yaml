argo-cd:
  server:
    ingress:
      enabled: true
      ingressClassName: nginx
      hostname: argocd.sloan.com
      tls: false
      #   nginx.ingress.kubernetes.io/backend-protocol: "HTTPS"
      #   nginx.ingress.kubernetes.io/ssl-redirect: "true"
  
  configs:
    params:
      "server.insecure": "true"

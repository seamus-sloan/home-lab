---
apiVersion: v1
kind: Service
metadata:
  name: iopaint
  namespace: default
  labels:
    app: iopaint
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
  selector:
    app: iopaint
